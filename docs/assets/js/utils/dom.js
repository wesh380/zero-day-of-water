export function waitForVisible(selector,{timeout=5000}={}){return new Promise((resolve,reject)=>{const t0=performance.now();(function tick(){const el=document.querySelector(selector);if(el&&el.offsetParent!==null)return resolve(el);if(performance.now()-t0>timeout)return reject(new Error(`waitForVisible timeout: ${selector}`));requestAnimationFrame(tick)})();});}
