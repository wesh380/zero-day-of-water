# راهنمای سیستم استایل داشبورد آب

این راهنما نحوه استفاده از سیستم استایل یکپارچه داشبورد آب را توضیح می‌دهد.

## 📋 فهرست مطالب

1. [پالت رنگی](#پالت-رنگی)
2. [انواع کارت‌ها](#انواع-کارتها)
3. [سیستم گرید](#سیستم-گرید)
4. [واکنش‌گرایی](#واکنشگرایی)
5. [نمونه‌های کاربردی](#نمونههای-کاربردی)

---

## 🎨 پالت رنگی

سیستم رنگی داشبورد آب شامل متغیرهای CSS زیر است:

### رنگ‌های اصلی آب
```css
--water-primary: #1E40AF;    /* آبی اصلی */
--water-secondary: #3B82F6;  /* آبی ثانویه */
--water-accent: #60A5FA;     /* رنگ تأکیدی */
--water-light: #DBEAFE;      /* آبی روشن */
```

### رنگ‌های وضعیت داده
```css
--water-critical: #EF4444;   /* بحرانی - قرمز */
--water-warning: #F59E0B;    /* هشدار - نارنجی */
--water-normal: #10B981;     /* عادی - سبز */
--water-low: #6B7280;        /* کم - خاکستری */
```

### گرادیانت‌های پس‌زمینه
```css
--water-gradient: linear-gradient(135deg, #1E40AF 0%, #3B82F6 100%);
--water-card-gradient: linear-gradient(180deg, #FFFFFF 0%, #F0F9FF 100%);
```

---

## 📦 انواع کارت‌ها

### 1. کارت اصلی (Primary Card)
برای نمایش اطلاعات اصلی و داده‌های کلیدی

**استفاده در React:**
```jsx
<Card variant="primary">
  <h3>عنوان کارت</h3>
  <p>محتوای کارت</p>
</Card>
```

**استفاده مستقیم CSS:**
```html
<div class="water-primary-card">
  محتوا
</div>
```

### 2. کارت هشدار (Alert Card)
برای نمایش هشدارها و وضعیت‌های بحرانی

**استفاده در React:**
```jsx
<Card variant="alert">
  <h3>هشدار بحران آب</h3>
  <p>پیام هشدار</p>
</Card>
```

**استفاده مستقیم CSS:**
```html
<div class="water-alert-card">
  محتوا
</div>
```

### 3. کارت پیش‌بینی (Future Card)
برای نمایش پیش‌بینی‌ها و اطلاعات آینده‌نگر

**استفاده در React:**
```jsx
<Card variant="future">
  <h3>پیش‌بینی بارش</h3>
  <p>۲۰ میلی‌متر</p>
</Card>
```

**استفاده مستقیم CSS:**
```html
<div class="water-future-card">
  محتوا
</div>
```

---

## 📐 سیستم گرید

داشبورد آب از سیستم گرید ۱۲ ستونه استفاده می‌کند.

### راه‌اندازی گرید
```jsx
<main className="water-dashboard-grid">
  {/* کارت‌های شما */}
</main>
```

### سایزبندی کارت‌ها

#### کارت کوچک (Small) - 3 ستون
```jsx
<Card size="small">محتوا</Card>
```

#### کارت متوسط (Medium) - 6 ستون
```jsx
<Card size="medium">محتوا</Card>
```

#### کارت بزرگ (Large) - 12 ستون
```jsx
<Card size="large">محتوا</Card>
```

#### کارت کامل (Full) - تمام عرض
```jsx
<Card size="full">محتوا</Card>
```

### ترکیب Variant و Size
```jsx
<Card variant="alert" size="full">
  <h2>هشدار مهم در تمام عرض</h2>
</Card>

<Card variant="primary" size="medium">
  <h3>اطلاعات اصلی</h3>
</Card>

<Card variant="future" size="small">
  <h4>پیش‌بینی کوچک</h4>
</Card>
```

---

## 📱 واکنش‌گرایی

سیستم استایل به صورت خودکار با سایزهای مختلف سازگار است.

### موبایل (< 768px)
- همه کارت‌ها به عرض کامل تبدیل می‌شوند
- فاصله‌ها کاهش می‌یابد (16px)
- Padding کمتر می‌شود

### تبلت (769px - 1024px)
- کارت‌های کوچک: 6 ستون
- کارت‌های متوسط: 12 ستون (تمام عرض)

### دسکتاپ (> 1024px)
- سیستم گرید 12 ستونه کامل
- فاصله‌ها استاندارد (24px)

---

## 💡 نمونه‌های کاربردی

### مثال 1: صفحه بحران آب
```jsx
export default function WaterCrisis() {
  return (
    <>
      <Header title="بحران آب" />
      <main className="water-dashboard-grid">
        <Card variant="alert" size="full">
          <h2>وضعیت بحرانی منابع آب</h2>
          <p>هشدارهای مهم...</p>
        </Card>

        <Card variant="primary" size="medium">
          <h3>آمار مصرف آب</h3>
        </Card>

        <Card variant="primary" size="medium">
          <h3>منابع آبی موجود</h3>
        </Card>

        <Card variant="future" size="small">
          <h4>پیش‌بینی بارش</h4>
          <p>۲۰ میلی‌متر</p>
        </Card>
      </main>
      <Footer />
    </>
  );
}
```

### مثال 2: صفحه پیش‌بینی آینده
```jsx
export default function FuturePrediction() {
  return (
    <main className="water-dashboard-grid">
      <Card variant="future" size="large">
        <h2>پیش‌بینی وضعیت منابع آب</h2>
      </Card>

      <Card variant="future" size="medium">
        <h3>پیش‌بینی بارندگی</h3>
      </Card>

      <Card variant="future" size="medium">
        <h3>تحلیل روند مصرف</h3>
      </Card>

      <Card variant="primary" size="small">
        <h4>دقت مدل: ۸۵٪</h4>
      </Card>
    </main>
  );
}
```

### مثال 3: استفاده مستقیم از CSS
```html
<div class="water-dashboard-grid">
  <div class="water-alert-card water-card-full">
    <h2>هشدار</h2>
  </div>

  <div class="water-primary-card water-card-medium">
    <h3>اطلاعات</h3>
  </div>

  <div class="water-future-card water-card-small">
    <h4>پیش‌بینی</h4>
  </div>
</div>
```

---

## 🎯 نکات مهم

1. **همیشه از `water-dashboard-grid` به عنوان container اصلی استفاده کنید**
2. **ترکیب variant و size برای کنترل کامل استایل**
3. **سیستم به صورت خودکار responsive است**
4. **از متغیرهای CSS برای سفارشی‌سازی رنگ‌ها استفاده کنید**
5. **برای استفاده در پروژه‌های غیر-React، از کلاس‌های CSS مستقیم استفاده کنید**

---

## 🔧 سفارشی‌سازی

برای سفارشی‌سازی رنگ‌ها، می‌توانید متغیرهای CSS را override کنید:

```css
:root {
  --water-primary: #YOUR_COLOR;
  --water-secondary: #YOUR_COLOR;
  /* ... */
}
```

---

## 📝 فایل‌های مرتبط

- **CSS اصلی:** `/docs/water/water.css`
- **کامپوننت Card:** `/dash/components/Card.js`
- **مثال صفحات:** `/dash/pages/water/`

---

**نسخه:** 1.0.0
**آخرین بروزرسانی:** ۱۴۰۳/۰۸/۰۴
