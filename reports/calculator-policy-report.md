# گزارش جامع سیاست و منطق ماشین‌حساب‌های سایت

**تاریخ تهیه**: 1403/07/30
**وضعیت**: نهایی
**تعداد ماشین‌حساب‌های مستند شده**: 9

---

## فهرست مطالب
1. [ماشین‌حساب قیمت تمام شده آب](#1-ماشین‌حساب-قیمت-تمام-شده-آب)
2. [ماشین‌حساب نیروگاه خورشیدی](#2-ماشین‌حساب-نیروگاه-خورشیدی)
3. [ماشین‌حساب قبض برق](#3-ماشین‌حساب-قبض-برق)
4. [ماشین‌حساب شدت کربن و ترکیب سوخت](#4-ماشین‌حساب-شدت-کربن-و-ترکیب-سوخت)
5. [داشبورد مصرف گاز و انرژی](#5-داشبورد-مصرف-گاز-و-انرژی)
6. [داشبورد اوج بار برق](#6-داشبورد-اوج-بار-برق)
7. [داشبورد کیفیت توزیع برق](#7-داشبورد-کیفیت-توزیع-برق)
8. [ماشین‌حساب آگروولتائیک](#8-ماشین‌حساب-آگروولتائیک)
9. [داشبورد مدیریت شبکه برق](#9-داشبورد-مدیریت-شبکه-برق)

---

## 1. ماشین‌حساب قیمت تمام شده آب

### 🎯 هدف و کاربرد
محاسبه قیمت واقعی هر مترمکعب آب شهری در مشهد با در نظر گرفتن تمام هزینه‌های مستقیم و غیرمستقیم، تلفات، و اثرات قطعی برق.

### 📍 مسیر فایل‌ها
- **HTML**: `/docs/water/cost-calculator.html`
- **JavaScript اصلی**: `/docs/water/cost-calculator.js`
- **JavaScript جایگزین**: `/docs/assets/cost-calculator.js`, `/docs/assets/water-cost-calculator.js`

### 📥 پارامترهای ورودی

| پارامتر | نوع | محدوده | مقدار پیش‌فرض | شرح |
|---------|-----|---------|---------------|------|
| `c_production` | عدد (تومان) | ≥0 | 2000 | هزینه تولید به ازای هر مترمکعب |
| `c_maintenance` | عدد (تومان) | ≥0 | 1500 | هزینه نگهداری و تعمیرات شبکه |
| `c_energy` | عدد (تومان) | ≥0 | 2000 | هزینه انرژی (برق و سوخت) |
| `p_loss` | درصد | 0-100 | 25 | درصد تلفات شبکه توزیع |
| `p_power_outage` | درصد | 0-100 | 15 | درصد اثر قطعی برق بر هزینه |

### 🧮 فرمول‌ها و منطق محاسبه

#### 1. محاسبه هزینه پایه
```javascript
base = c_production + c_maintenance + c_energy
```

#### 2. محاسبه هزینه واقعی (با احتساب تلفات)
```javascript
realCost = base / (1 - p_loss / 100)
```
**منطق**: تلفات شبکه باعث می‌شود برای رساندن 1 مترمکعب آب به مشترک، باید بیشتر تولید کرد.

#### 3. محاسبه قیمت نهایی (با احتساب قطعی برق)
```javascript
finalPrice = realCost × (1 + p_power_outage / 100)
```
**منطق**: قطعی برق هزینه‌های اضافی (تجهیزات یدکی، ژنراتور، از دست رفتن آب) به سیستم تحمیل می‌کند.

#### 4. محاسبه تحلیل حساسیت
برای هر پارامتر ورودی:
```javascript
newValue = currentValue × 1.1  // افزایش 10%
newFinalPrice = calculateWithNewValue(newValue)
impact = ((newFinalPrice - currentFinalPrice) / currentFinalPrice) × 100
```

### 📤 خروجی‌ها

| خروجی | واحد | شرح |
|-------|------|------|
| `realCost` | تومان/م³ | هزینه واقعی با احتساب تلفات |
| `finalPrice` | تومان/م³ | قیمت پیشنهادی نهایی |
| جدول تفکیک هزینه | - | نمایش سهم هر عامل از هزینه کل |
| نمودار دونات | - | نمایش بصری سهم هزینه‌ها |
| تحلیل حساسیت | - | رتبه‌بندی تأثیر افزایش 10% هر عامل |

### 🔐 سیاست‌ها و محدودیت‌ها

1. **محدودیت ورودی**:
   - تمام مقادیر مالی باید غیر منفی باشند
   - درصدها در بازه [0, 100] محدود می‌شوند
   - ورودی‌های فارسی به انگلیسی تبدیل می‌شوند

2. **اعتبارسنجی**:
   - شناسایی و نمایش خطا برای ورودی‌های نامعتبر
   - جلوگیری از محاسبه با مقادیر NaN

3. **تجربه کاربری**:
   - محاسبه خودکار با debounce 200ms
   - همگام‌سازی slider و input
   - ذخیره‌سازی در localStorage (نسخه دوم)
   - قابلیت Reset به مقادیر پیش‌فرض

4. **قالب‌بندی خروجی**:
   - اعداد فارسی با جداکننده هزارگان
   - نمایش "تومان" به صورت صریح
   - درصدها با 1 رقم اعشار

---

## 2. ماشین‌حساب نیروگاه خورشیدی

### 🎯 هدف و کاربرد
تحلیل مالی جامع پروژه‌های نیروگاه خورشیدی با محاسبه NPV، IRR، دوره بازگشت سرمایه، و مقایسه با جریمه عدم تأمین انرژی تجدیدپذیر.

### 📍 مسیر فایل‌ها
- **JavaScript**: `/docs/assets/js/solar-plant-calc.js`
- **Config**: `/docs/config/solar-calculator.json`
- **HTML**: `/docs/solar/plant/index.html`

### 📥 پارامترهای ورودی

#### الزامات قانونی (Legal)
| پارامتر | نوع | پیش‌فرض | شرح |
|---------|-----|---------|------|
| `isObliged` | Boolean | true | آیا مشمول الزام انرژی تجدیدپذیر است؟ |
| `rampPerYearPct` | % | - | نرخ افزایش سالانه الزام |
| `capSharePct` | % | - | حداکثر درصد سهم الزامی |
| `startYear` | سال | - | سال شروع الزام |

#### قیمت‌گذاری (Pricing)
| پارامتر | نوع | شرح |
|---------|-----|------|
| `greenBoard` | ریال/kWh | قیمت بورس سبز |
| `greenBoardGrowthPct` | % | نرخ رشد سالانه قیمت بورس |
| `gridPrice` | ریال/kWh | قیمت شبکه (اختیاری) |

#### مالی (Finance)
| پارامتر | نوع | شرح |
|---------|-----|------|
| `capexPerKW` | ریال | هزینه سرمایه‌گذاری به ازای هر کیلووات |
| `specificYield` | kWh/kW | بازده ویژه سالانه |
| `prLossPct` | % | ضریب تلفات سیستم (Performance Ratio Loss) |
| `omPctOfRevenue` | % | هزینه O&M به درصد درآمد |
| `discountPct` | % | نرخ تنزیل (Discount Rate) |
| `horizonYears` | سال | افق زمانی پروژه |

#### سناریو
| پارامتر | نوع | شرح |
|---------|-----|------|
| `year` | سال | سال شروع پروژه |
| `annualConsumption` | kWh | مصرف سالانه برق |
| `capacityKW` | kW | ظرفیت نیروگاه |
| `projectType` | String | نوع پروژه (rooftop, ground, ...) |

### 🧮 فرمول‌ها و منطق محاسبه

#### 1. محاسبه انرژی تولیدی سالانه
```javascript
grossEnergy = capacityKW × specificYield
netEnergy = grossEnergy × (1 - prLossPct / 100)
```

#### 2. محاسبه الزام سالانه
```javascript
yearsFromStart = year - startYear + 1
requiredSharePct = min(capSharePct, yearsFromStart × rampPerYearPct)
requiredEnergy = (requiredSharePct / 100) × annualConsumption
```

#### 3. محاسبه جریمه سالانه
```javascript
shortfall = max(0, requiredEnergy - netEnergy)
penaltyPrice_year = greenBoard × (1 + greenBoardGrowthPct/100)^year
penalty_year = shortfall × penaltyPrice_year
```

#### 4. محاسبه درآمد سالانه
```javascript
if (gridPrice is set):
    revenue_year = netEnergy × gridPrice
else:
    revenue_year = netEnergy × greenBoard × (1 + greenBoardGrowthPct/100)^year
```

#### 5. محاسبه جریان نقدی
```javascript
cashflow[0] = -capexTotal  // سرمایه‌گذاری اولیه
for year in 1..horizonYears:
    om_year = revenue_year × (omPctOfRevenue / 100)
    cashflow[year] = revenue_year - om_year
```

#### 6. محاسبه NPV
```javascript
discountedCashflow[i] = cashflow[i] / (1 + discountPct/100)^i
NPV = sum(discountedCashflow)
```

#### 7. محاسبه IRR
با استفاده از الگوریتم Bisection Method:
```javascript
find r such that: NPV(r) = 0
where NPV(r) = Σ cashflow[i] / (1+r)^i = 0
```

#### 8. محاسبه دوره بازگشت سرمایه
```javascript
cumulative = 0
for year in 0..horizonYears:
    cumulative += cashflow[year]
    if cumulative >= 0:
        fraction = (0 - previousCumulative) / cashflow[year]
        payback = year - 1 + fraction
        break
```

### 📤 خروجی‌ها

| خروجی | واحد | شرح |
|-------|------|------|
| `totalCapex` | ریال | کل سرمایه‌گذاری |
| `firstYearRevenue` | ریال | درآمد سال اول |
| `firstYearOM` | ریال | هزینه O&M سال اول |
| `producedEnergy` | kWh | انرژی تولیدی سالانه |
| `requiredEnergy` | kWh | انرژی مورد نیاز (الزام) سال اول |
| `simplePaybackYears` | سال | دوره بازگشت سرمایه ساده |
| `discountedPaybackYears` | سال | دوره بازگشت سرمایه تنزیل‌شده |
| `NPV` | ریال | خالص ارزش فعلی |
| `IRR` | % | نرخ بازده داخلی |
| `totalPenalty` | ریال | مجموع جریمه در طول عمر پروژه |

### 🔐 سیاست‌ها و محدودیت‌ها

1. **الگوریتم IRR**:
   - Tolerance: 1e-7
   - حداکثر تکرار: 100
   - محدوده جستجو: [-0.99, 1.0]
   - در صورت عدم همگرایی: null

2. **اعتبارسنجی ورودی**:
   - تمام اعداد به فارسی نرمال می‌شوند
   - جداکننده هزارگان (,) حذف می‌شود
   - افق زمانی حداقل 1 سال

3. **مدیریت قیمت**:
   - اگر gridPrice تنظیم شود: ثابت می‌ماند
   - در غیر این صورت: از greenBoard با رشد سالانه

4. **محاسبه الزام**:
   - فقط برای isObliged=true
   - Ramp محدود به Cap
   - shortfall نمی‌تواند منفی باشد

---

## 3. ماشین‌حساب قبض برق

### 🎯 هدف و کاربرد
محاسبه دقیق قبض برق خانگی بر اساس تعرفه پله‌ای، ارائه تحلیل مصرف، و شبیه‌سازی صرفه‌جویی.

### 📍 مسیر فایل‌ها
- **JavaScript**: `/docs/assets/power-tariff.js`
- **CSS**: `/docs/assets/power-tariff.css`
- **HTML**: `/docs/electricity/power-tariff.html`

### 📥 پارامترهای ورودی

| پارامتر | نوع | شرح |
|---------|-----|------|
| `totalKwh` | عدد | مصرف کل برق (کیلووات‌ساعت) |
| `previousBalance` | ریال | بدهی دوره قبل |
| `savingsPercent` | % (0-100) | درصد کاهش مصرف برای what-if |

### 🧮 سیستم تعرفه پله‌ای

```javascript
const tariffBlocks = [
  { from: 0,   to: 100, rate: 600   },  // پله 1
  { from: 100, to: 200, rate: 700   },  // پله 2
  { from: 200, to: 300, rate: 1500  },  // پله 3
  { from: 300, to: 400, rate: 2500  },  // پله 4
  { from: 400, to: 500, rate: 3000  },  // پله 5
  { from: 500, to: 600, rate: 3500  },  // پله 6
  { from: 600, to: ∞,   rate: 4000  }   // پله 7
]
```

### 🧮 فرمول‌های محاسبه

#### 1. محاسبه هزینه انرژی
```javascript
energyCost = 0
remaining = totalKwh

for each block:
    consumption_in_block = min(remaining, block.to - block.from)
    energyCost += consumption_in_block × block.rate
    remaining -= consumption_in_block
```

#### 2. محاسبه عوارض و مالیات
```javascript
fixedCharge = 30000  // ریال
levies = energyCost × 10%  // عوارض
vat = (energyCost + fixedCharge + levies) × 9%  // مالیات بر ارزش افزوده
```

#### 3. محاسبه کل قبض
```javascript
totalBill = energyCost + fixedCharge + levies + vat + previousBalance
```

#### 4. تحلیل what-if (صرفه‌جویی)
```javascript
newKwh = totalKwh × (1 - savingsPercent / 100)
newBill = calculateBill(newKwh) + previousBalance
savings = totalBill - newBill
```

### 📤 خروجی‌ها

| خروجی | واحد | شرح |
|-------|------|------|
| `energyCost` | ریال | هزینه مصرف برق |
| `fixedCharge` | ریال | هزینه ثابت اشتراک |
| `levies` | ریال | عوارض (10%) |
| `vat` | ریال | مالیات بر ارزش افزوده (9%) |
| `totalBill` | ریال | جمع کل قبض |
| تشخیص پله | - | شناسایی پله تعرفه‌ای کاربر |
| تحلیل مصرف | - | وضعیت کم‌مصرف/متوسط/پرمصرف |
| صرفه‌جویی | ریال | میزان صرفه‌جویی با کاهش مصرف |

### 🔐 سیاست‌ها و محدودیت‌ها

1. **ثوابت تعرفه**:
   - عوارض: 10% از هزینه انرژی
   - مالیات: 9% از (انرژی + ثابت + عوارض)
   - هزینه ثابت: 30,000 ریال

2. **تحلیل مصرف**:
   - ≤200 kWh: کم‌مصرف
   - 200-400 kWh: متوسط
   - >400 kWh: پرمصرف

3. **قابلیت‌های اضافی**:
   - PDF export با html2canvas و jsPDF
   - سیستم تب‌بندی
   - نمایش بصری با نشانگر موقعیت

---

## 4. ماشین‌حساب شدت کربن و ترکیب سوخت

### 🎯 هدف و کاربرد
محاسبه شدت کربن شبکه برق بر اساس ترکیب سوخت‌های مصرفی و تحلیل ردپای کربن لوازم برقی.

### 📍 مسیر فایل‌ها
- **JavaScript**: `/docs/assets/js/gas-fuel-carbon.js`
- **HTML**: `/docs/gas/fuel-carbon.html`

### 📥 پارامترهای ورودی

#### ترکیب سوخت (Fuel Mix)
| پارامتر | نوع | محدوده | شرح |
|---------|-----|---------|------|
| `gas` | % | 0-100 | سهم گاز طبیعی |
| `diesel` | % | 0-100 | سهم گازوئیل |
| `mazut` | % | 0-100 | سهم مازوت |
| `other` | % | 0-100 | سایر سوخت‌ها |

#### لوازم برقی (برای محاسبه ردپای کربن)
- انتخاب لوازم از لیست پیش‌تعریف شده
- توان مصرفی (kW) از دیتابیس

### 🧮 ضرایب انتشار (Emission Factors)

```javascript
const emissionFactors = {
  gas:    0.35,  // kgCO₂e/kWh
  diesel: 0.65,  // kgCO₂e/kWh
  mazut:  0.75,  // kgCO₂e/kWh
  other:  0.50   // kgCO₂e/kWh
}
```

### 🧮 فرمول‌های محاسبه

#### 1. شدت کربن شبکه
```javascript
carbonIntensity =
  (gas/100 × 0.35) +
  (diesel/100 × 0.65) +
  (mazut/100 × 0.75) +
  (other/100 × 0.50)
```

#### 2. ردپای کربن لوازم
```javascript
CO2_grams_per_hour = appliancePowerKW × carbonIntensity × 1000
```

#### 3. شبیه‌سازی افزایش گاز
```javascript
addedGas = sliderValue  // درصد

// اول مازوت را جایگزین کن
mazutReduction = min(currentMazut, addedGas)
newMazut = currentMazut - mazutReduction
remaining = addedGas - mazutReduction

// بقیه را از گازوئیل کم کن
if (remaining > 0):
    newDiesel = currentDiesel - min(currentDiesel, remaining)

newGas = currentGas + addedGas
```

#### 4. محاسبه کاهش انتشار
```javascript
originalIntensity = calculateIntensity(originalMix)
newIntensity = calculateIntensity(newMix)
reductionPercent = ((originalIntensity - newIntensity) / originalIntensity) × 100
```

### 📤 KPI ها و شاخص‌ها

| شاخص | واحد | آستانه‌ها | شرح |
|------|------|-----------|------|
| `gasShare` | % | - | سهم گاز در ترکیب سوخت |
| `mazutShare` | % | >10 هشدار، >20 خطر | سهم مازوت |
| `carbonIntensity` | kgCO₂e/kWh | - | شدت کربن |
| `yoyChange` | % | مثبت=افزایش، منفی=کاهش | تغییر سال به سال |
| `mazutFreeDays` | روز | - | تعداد روزهای بدون مازوت |

### 🔐 سیاست‌ها

1. **آستانه‌های هشدار مازوت**:
   - <10%: وضعیت مطلوب (سبز)
   - 10-20%: هشدار (زرد)
   - >20%: خطر (قرمز)

2. **اولویت جایگزینی سوخت**:
   - اول مازوت (آلاینده‌ترین)
   - سپس گازوئیل
   - در آخر سایر سوخت‌ها

3. **نمایش‌ها**:
   - 2 نمودار Doughnut (استانی و ملی)
   - نمودار خطی روند شدت کربن
   - رنگ‌بندی بر اساس وضعیت

---

## 5. داشبورد مصرف گاز و انرژی

### 🎯 هدف و کاربرد
نمایش جامع تراز گاز، مصرف بخشی، Days of Cover (DOC) فرآورده‌ها، و مقایسه مصرف کاربر با میانگین.

### 📍 مسیر فایل‌ها
- **JavaScript**: `/docs/gas/energy.js`
- **HTML**: `/docs/gas/energy.html`

### 📥 فیلترها

| فیلتر | گزینه‌ها | شرح |
|-------|----------|------|
| `period` | monthly, quarterly, annual | دوره زمانی |
| `city` | all, mashhad, sabzevar, ... | محدوده جغرافیایی |
| `product` | all, gasoline, diesel, lpg | نوع فرآورده |

### 🧮 فرمول‌های محاسبه

#### 1. تراز گاز (Gas Balance)
```javascript
gasBalance = gasInput - totalConsumption
```
رنگ‌بندی:
- سبز: balance ≥ 0
- قرمز: balance < 0

#### 2. مصرف بخشی
```javascript
sectors = {
  household: 10 MCM,
  industry: 7 MCM,
  power_plant: 4 MCM,
  public: 1 MCM,
  agriculture: 0.5 MCM
}
```

#### 3. Days of Cover (DOC)
```javascript
DOC = (available_stock / daily_consumption)
```

مقادیر نمونه:
- بنزین: 15 روز
- گازوئیل: 21 روز
- LPG: 30 روز

#### 4. مقایسه مصرف خانگی
```javascript
averageHouseholdConsumption = 350  // m³

if (userConsumption - average) / average > 10%:
    status = "پرمصرف"
else if (userConsumption - average) / average < -10%:
    status = "کم‌مصرف"
else:
    status = "در محدوده میانگین"
```

### 📤 نمایش‌های بصری

1. **نمودار آبشاری (Waterfall)**:
   - ورودی گاز
   - کاهش به ازای هر بخش
   - تراز نهایی

2. **نمودار دونات**:
   - سهم هر بخش از مصرف

3. **نمودار ستونی تراکمی**:
   - تولید داخلی vs واردات
   - برای هر فرآورده

4. **نمودار خطی روند**:
   - 24 ماه گذشته
   - مصرف گاز (محور چپ)
   - مصرف بنزین (محور راست)

### 🔐 سیاست‌ها

1. **فیلتر شهرها**:
   - فقط شهرهای بالای 1000 مشترک نمایش
   - شهرهای کوچک در "کل استان" جمع می‌شوند

2. **نکات صرفه‌جویی**:
   - 6 نکته تصادفی
   - تازه‌سازی با هر بارگذاری

3. **وضعیت شبکه**:
   - سبز (normal): مصرف عادی
   - زرد (high): مصرف بالا
   - قرمز (critical): اوج مصرف

---

## 6. داشبورد اوج بار برق

### 🎯 هدف و کاربرد
نمایش تقاضا و مصرف ساعتی برق، تشخیص ساعت اوج، و تحلیل مصرف خانگی.

### 📍 مسیر فایل‌ها
- **JavaScript**: `/docs/assets/electricity-peak.js`
- **HTML**: `/docs/electricity/peak.html`

### 📥 داده‌های ورودی

#### داده‌های ساعتی (6:00 - 24:00)
```javascript
consumption = [3200, 3300, 3400, ..., 3150]  // MW
demand = [3300, 3400, 3550, ..., 3250]  // MW
```

#### تفکیک مصرف خانگی
```javascript
householdBreakdown = {
  heating_cooling: 45%,
  lighting: 25%,
  appliances: 20%,
  other: 10%
}
```

### 🧮 محاسبات

#### 1. ضریب بار (Load Factor)
```javascript
peakDemand = max(demand)
averageDemand = mean(demand)
loadFactor = (averageDemand / peakDemand) × 100
```
مقدار نمونه: 78%

#### 2. شناسایی ساعت اوج
```javascript
peakHour = findIndexOfMax(demand)
```

#### 3. مصرف شهری
برای 8 شهر برتر استان (MW):
- مشهد: 120
- نیشابور: 95
- سبزوار: 80
- تربت حیدریه: 65
- تویسرکان: 50
- قوچان: 40
- کاشمر: 35
- کلات: 25

### 📤 نمایش‌ها

1. **نمودار خطی**:
   - محور X: ساعت (6-24)
   - محور Y: مگاوات
   - دو خط: مصرف (آبی) و تقاضا (نارنجی)

2. **نمودار دونات**:
   - تفکیک مصرف خانگی

3. **نمودار میله‌ای افقی**:
   - مصرف شهری (رتبه‌بندی شده)

### 🔐 سیاست‌ها

1. **راهنمای زمان مصرف**:
   - ساعات اوج: کاهش مصرف
   - ساعات خارج از اوج: مصرف بهینه

2. **قالب‌بندی**:
   - اعداد فارسی
   - RTL در تمام نمودارها
   - فونت Vazirmatn

---

## 7. داشبورد کیفیت توزیع برق

### 🎯 هدف و کاربرد
محاسبه و نمایش شاخص‌های کیفیت توزیع برق (SAIDI, SAIFI, CAIDI) برای شهرهای مختلف.

### 📍 مسیر فایل‌ها
- **JavaScript**: `/docs/assets/electricity-quality.js`
- **HTML**: `/docs/electricity/quality.html`

### 📥 شاخص‌های کیفیت

#### SAIDI (System Average Interruption Duration Index)
```
SAIDI = مجموع (دقیقه قطعی × تعداد مشترکین متأثر) / کل مشترکین
واحد: دقیقه/مشترک/سال
```

#### SAIFI (System Average Interruption Frequency Index)
```
SAIFI = مجموع (تعداد قطعی × مشترکین متأثر) / کل مشترکین
واحد: تعداد/مشترک/سال
```

#### CAIDI (Customer Average Interruption Duration Index)
```
CAIDI = SAIDI / SAIFI
واحد: دقیقه/قطعی
```

### 🧮 محاسبات تفکیکی

#### قطعی برنامه‌ریزی شده vs غیربرنامه
```javascript
plannedShare = (plannedOutageMinutes / totalOutageMinutes) × 100
unplannedShare = 100 - plannedShare
```

### 📤 KPI ها

| شاخص | واحد | آستانه مطلوب | شرح |
|------|------|--------------|------|
| SAIDI | دقیقه | <200 | میانگین مدت قطعی |
| SAIFI | تعداد | <3 | میانگین تعداد قطعی |
| CAIDI | دقیقه | <60 | میانگین طول هر قطعی |

### 🔐 سیاست رنگ‌بندی

```javascript
if (SAIDI < 150): status = "عالی" (سبز)
else if (SAIDI < 250): status = "خوب" (زرد)
else: status = "نیاز به بهبود" (قرمز)
```

### 📤 نمایش‌ها

1. **نمودار خطی روند ماهانه**:
   - SAIDI, SAIFI, CAIDI
   - 12 ماه گذشته

2. **جدول تفکیک شهری**:
   - هر شاخص برای هر شهر
   - رنگ‌بندی بر اساس وضعیت

3. **نمودار ستونی**:
   - سهم قطعی برنامه‌ریزی شده vs غیربرنامه

---

## 8. ماشین‌حساب آگروولتائیک

### 🎯 هدف و کاربرد
محاسبه سودآوری سیستم‌های ترکیبی کشاورزی-خورشیدی (Agrivoltaics) با قابلیت صادرات PDF.

### 📍 مسیر فایل‌ها
- **JavaScript**: `/docs/solar/agrivoltaics/app.js` (React-based)
- **HTML**: `/docs/solar/agrivoltaics/index.html`
- کتابخانه‌ها: React, React-DOM, QRCode, jsPDF

### 📥 پارامترهای ورودی

#### کشاورزی
| پارامتر | واحد | شرح |
|---------|------|------|
| `landArea` | هکتار | مساحت زمین |
| `cropYield` | تن/هکتار | عملکرد محصول |
| `cropPrice` | ریال/تن | قیمت محصول |

#### خورشیدی
| پارامتر | واحد | شرح |
|---------|------|------|
| `solarCapacity` | kW | ظرفیت پنل‌ها |
| `shadingFactor` | % | ضریب سایه‌اندازی |
| `electricityPrice` | ریال/kWh | قیمت برق |

### 🧮 فرمول‌های محاسبه

#### 1. درآمد کشاورزی
```javascript
agriculturalRevenue = landArea × cropYield × cropPrice × (1 - shadingFactor/100)
```

#### 2. تولید برق
```javascript
annualProduction = solarCapacity × specificYield × (1 - shadingLoss)
```

#### 3. درآمد برق
```javascript
solarRevenue = annualProduction × electricityPrice
```

#### 4. درآمد ترکیبی
```javascript
totalRevenue = agriculturalRevenue + solarRevenue
```

### 📤 خروجی‌ها

- محاسبات مالی جامع
- مقایسه با کشاورزی خالص
- صادرات PDF با QR Code
- ارسال job به سرور و دریافت نتیجه

### 🔐 سیاست‌ها

1. **معماری React Component-Based**
2. **Async Job Submission** با polling
3. **PDF Export** با QR code برای اشتراک‌گذاری
4. **Responsive Design**

---

## 9. داشبورد مدیریت شبکه برق

### 🎯 هدف و کاربرد
مدیریت کلی شبکه برق با نمایش ضریب بار، پیش‌بینی اوج، و راهنمای زمان مصرف.

### 📍 مسیر فایل‌ها
- **JavaScript**: `/docs/assets/electricity-management.js`
- **HTML**: `/docs/electricity/peak.html`

### 🧮 محاسبات

#### 1. ضریب بار شبکه
```javascript
networkLoadFactor = (totalConsumption / peakCapacity) × 100
```
مقدار نمونه: 78%

#### 2. وضعیت بار
```javascript
if (currentLoad / peakCapacity < 0.7): status = "normal"
else if (currentLoad / peakCapacity < 0.9): status = "high"
else: status = "critical"
```

### 📤 راهنمای زمان مصرف

| وضعیت | رنگ | پیام | اقدام |
|-------|-----|------|-------|
| Normal | سبز | مصرف عادی | استفاده معمولی |
| High | زرد | مصرف بالا | کاهش مصرف |
| Critical | قرمز | اوج بار | حداقل مصرف |

---

## خلاصه آمار و ارقام

### تعداد ماشین‌حساب‌ها به تفکیک حوزه
- **آب**: 1
- **برق**: 4
- **گاز و انرژی**: 2
- **خورشید**: 2

### تکنولوژی‌های استفاده شده
- **JavaScript**: ES6+ (تمام پروژه‌ها)
- **Chart.js**: 8 پروژه
- **React.js**: 1 پروژه (Agrivoltaics)
- **localStorage**: 2 پروژه
- **PDF Export**: 2 پروژه
- **Tailwind CSS**: تمام پروژه‌ها

### قابلیت‌های مشترک
- ✅ پشتیبانی کامل از فارسی (RTL)
- ✅ اعتبارسنجی ورودی
- ✅ نمایش بصری (نمودارها)
- ✅ Responsive Design
- ✅ تحلیل حساسیت / What-if
- ✅ قالب‌بندی اعداد فارسی

### محدودیت‌های طراحی
- تمام محاسبات Client-Side
- استفاده از داده‌های Mock/Sample
- عدم اتصال به API واقعی (به جز Agrivoltaics)
- ذخیره‌سازی محلی (localStorage)

---

## توصیه‌ها برای توسعه آینده

### 1. یکپارچه‌سازی
- ایجاد یک کتابخانه مشترک برای فرمول‌ها
- استانداردسازی format اعداد
- یکپارچه‌سازی سیستم تم و رنگ‌بندی

### 2. بک‌اند
- ایجاد API برای ذخیره/بازیابی محاسبات
- اتصال به منابع داده واقعی
- امکان مقایسه تاریخی

### 3. امنیت
- اعتبارسنجی سمت سرور
- Rate limiting برای محاسبات
- Sanitization ورودی‌ها

### 4. عملکرد
- Lazy loading برای Chart.js
- Code splitting
- استفاده از Web Workers برای محاسبات سنگین

### 5. تجربه کاربری
- پیشنهادهای هوشمند
- مقایسه با سایر کاربران
- گزارش‌های قابل اشتراک‌گذاری

---

**پایان گزارش**
تهیه شده توسط: Claude Code
تاریخ: 1403/07/30
